import { createSelector } from 'reselect';
import { INITIAL_STATE } from '../reducers/{{ camelCase name }}';

/**
 * Direct selector to the {{ camelCase name }} state domain
 */

const select{{ properCase name }}Domain = state => state.{{ camelCase name }} || INITIAL_STATE;


const makeSelect{{ properCase name }} = () =>
  createSelector(
    select{{ properCase name }}Domain,
    substate => substate,
  );

export default makeSelect{{ properCase name }};
export { select{{ properCase name }}Domain };
